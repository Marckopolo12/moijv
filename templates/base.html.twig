<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Moijv - {% block title %}Dashboard{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    
</head>
<header>    
<nav>
        <ul>
            <li><a href="{{ path('list_user')}}">Liste des utilisateurs</a></li>
            <li><a href="{{ path('list_product')}}">Liste des produits</a></li>
            {% if is_granted ('ROLE_USER')%} {# vérifie les perms de l'utilisateur #}
            <li><a href="{{ path('my_products')}}">Mes produit</a></li>
            <li><a href="{{ path('add_product')}}">Ajouter un produit</a></li>
           {% endif %}
            
            <li>a</li>
            <li>a</li>
            <li>a</li>
        <ul>    
    </nav>
</header>    
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>

